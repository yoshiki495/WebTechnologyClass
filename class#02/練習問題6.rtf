const order = ['LLA', 'RLL', 'RLA', 'LLL'];
let speed = 1
for(let i=0; i<10; i++){
  pic.R(order[i%2],-45,speed)
  pic.RW(order[i%2+1],45,speed)
  pic.R(order[i%2],45,speed)
  pic.RW(order[i%2+1],-45,speed)
  speed = speed -0.1
}
